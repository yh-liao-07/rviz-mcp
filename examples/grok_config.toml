# Paste into ~/.grok/config.toml or project .grok/config.toml
# Requires: pip install "git+https://github.com/mergeos-bounties/rviz-mcp.git"

[mcp_servers.rviz_mcp]
command = "rviz-mcp"
args = ["serve"]
env = { RVIZ_MCP_MODE = "mock" }
enabled = true
